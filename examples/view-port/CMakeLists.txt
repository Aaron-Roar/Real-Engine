cmake_minimum_required(VERSION 3.20)
if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
    project(rohr_view_port LANGUAGES C)
endif()
include(${CMAKE_CURRENT_LIST_DIR}/../cmake/RohrExample.cmake)
rohr_example_bootstrap()

add_executable(view_port src/main.c src/elderfly_descriptors.c)
rohr_add_example_runtime(view_port)
target_include_directories(view_port PRIVATE src)
target_link_libraries(view_port PRIVATE rohr_engine)
rohr_stage_example_assets(view_port view-port)
