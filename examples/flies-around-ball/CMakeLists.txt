cmake_minimum_required(VERSION 3.20)
if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
    project(rohr_flies_around_ball LANGUAGES C)
endif()
include(${CMAKE_CURRENT_LIST_DIR}/../cmake/RohrExample.cmake)
rohr_example_bootstrap()

add_executable(flies_around_ball src/main.c src/elderfly_descriptors.c)
rohr_add_example_runtime(flies_around_ball)
target_include_directories(flies_around_ball PRIVATE src)
target_link_libraries(flies_around_ball PRIVATE rohr_engine)
rohr_stage_example_assets(flies_around_ball flies-around-ball)
